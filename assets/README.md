# Assets

- Add your **resume PDF** here as `resume.pdf` so the "Resume" button in the header works.
- You can also add a profile image and reference it in the About section (e.g. replace the avatar placeholder in `index.html` with an `<img>`).
